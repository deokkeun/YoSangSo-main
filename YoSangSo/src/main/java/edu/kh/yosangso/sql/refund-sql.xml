<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<comment>refund-sql.xml</comment>
	<entry key="orderList">
		SELECT PRODUCT_NM, ORDER_NO, ORDER_DETAIL.PRICE PRICE, BUYING_RATE FROM ORDER_DETAIL JOIN PRODUCT USING (PRODUCT_NO) JOIN "ORDER" USING(ORDER_NO) JOIN MEMBER USING(MEMBER_NO) WHERE MEMBER_NO = ? AND REFUND_FL = 'N'
	</entry>
	<entry key="refundDone">
		INSERT INTO REFUND VALUES(REFUND_SEQ.NEXTVAL, ?, '사유', DEFAULT)
	</entry>
	<entry key="updateOrder">
		UPDATE "ORDER" SET REFUND_FL = 'Y' WHERE ORDER_NO = ? 
	</entry>
	<entry key="refundList">
		SELECT PRODUCT_NM, ORDER_NO, ORDER_DETAIL.PRICE PRICE, BUYING_RATE, ORDER_DATE , TO_CHAR(REFUND_DATE, 'YY/MM/DD') REFUND_DATE FROM ORDER_DETAIL JOIN PRODUCT USING (PRODUCT_NO) JOIN "ORDER" USING(ORDER_NO) JOIN MEMBER USING(MEMBER_NO) JOIN REFUND USING(ORDER_DETAIL_NO) WHERE MEMBER_NO = ? AND REFUND_FL = 'Y'
	</entry>
</properties>